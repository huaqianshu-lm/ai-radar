# AI Radar 前端页面设计参考

目前前端不需要做复杂的“AI 情报工作台”或“每日简报 Dashboard”。

第一版目标应该尽量简单：

**把已经抓取到的 AI 新闻，以清晰、舒服、适合连续阅读的方式展示出来。**

## 一、页面核心内容

每条新闻只需要展示：

- 标题
- 短摘要
- 来源
- 来源类型
- 时间；优先显示真实发布时间，没有时明确显示收录时间

前端应读取 AI Radar 提供的稳定前端数据，不直接依赖内部 items JSONL。对应字段是：

```text
id
title
summary
source
source_type
display_time
time_type
url
```

其中：

- `title`：中文新闻标题；产品名、模型名和机构名可以保留原文
- `summary`：中文新闻短摘要；清理抓取模板和无关噪声，不补写原始材料没有提供的事实
- `source`：来源名称
- `source_type`：来源类型
- `display_time`：已经由 AI Radar 统一处理的显示和排序时间
- `time_type`：`published` 表示真实发布时间，`fetched` 表示没有发布时间时使用的收录时间
- `url`：点击新闻后打开原文

第一版暂时不要展示：

```text
credibility_score
cluster_size
is_list_page
importance_score
relevance_score
category
should_archive
should_write
cluster_key
cluster_basis
cluster_rank
raw_path
```

这些字段要么属于内部处理信息，要么目前数据还不够成熟，不适合直接进入前端。

---

# 二、新闻列表结构

推荐使用：

**列表 + 留白 + 分割线**

不要做大量独立大卡片。

单条新闻大致结构：

```text
新闻标题

两到三行短摘要内容……

OpenAI News · 官方 · 10:43

没有真实发布时间时：

GitHub Trending · 开发者 · 收录于 10:43
```

不同新闻之间使用细分割线。

例如：

```text
OpenAI 发布新的模型能力

新版本进一步提升代码生成、工具调用以及
长任务处理能力，并针对开发者场景进行了优化。

OpenAI News · 官方 · 10:43


────────────────────────────


Anthropic 更新 Claude Code

本次更新主要围绕 Agent 执行、上下文管理
以及长时间开发任务进行了改进。

Anthropic · 官方 · 09:36
```

目标是：

**高信息密度，但阅读体验干净。**

---

# 三、来源分类

目前不建议做：

```text
模型
Agent
AI 编程
产品
研究
```

因为现有 `category` 数据还不成熟，目前基本属于：

```text
pending_manual_review
```

第一版只根据现有的：

```text
source_type
```

做来源过滤。

例如：

```text
全部
官方
媒体
社区
```

需要注意：

具体有哪些分类，不要直接假定。

应该先检查真实项目里 `source_type` 当前到底有哪些枚举值。

例如可能是：

```text
official
media
community
newsletter
research
```

也可能是其他命名方式。

前端分类应该根据真实数据决定。

映射后可以显示：

```text
official     → 官方
media        → 媒体
community    → 社区
newsletter   → Newsletter
research     → 研究
```

如果实际项目没有某一类，就不要为了页面设计强行加入。

---

# 四、“最新”不是分类

顶部可以设计成类似：

```text
全部    官方    媒体    社区
```

新闻默认按照 `display_time` 倒序排列。

时间规则：

```text
time_type = published
→ 显示真实发布时间，例如“10:43”
```

如果来源没有提供真实发布时间：

```text
time_type = fetched
→ 明确显示“收录于 10:43”
```

不要把 `fetched` 类型的时间直接显示成发布时间。GitHub Trending 等没有文章发布时间的来源同样使用收录时间。

因此不需要额外设计一个复杂的“最新”分类。

默认首页本身就是最新新闻流。

---

# 五、页面结构建议

第一版甚至可以只有一个主要页面。

例如：

```text
AI RADAR

每天值得关注的 AI 动态


[全部] [官方] [媒体] [社区]          搜索


TODAY
AUGUST 11


新闻标题

短摘要……

OpenAI News · 官方 · 10:43


────────────────────────


新闻标题

短摘要……

TechCrunch · 媒体 · 09:21


────────────────────────
```

如果历史数据比较多，可以增加简单日期切换：

```text
今天
昨天
8 月 9 日
8 月 8 日
```

但不要一开始设计复杂的多页面信息架构。

---

# 六、视觉方向

整体不建议做常见的：

```text
AI 蓝紫渐变
发光效果
大面积玻璃卡片
机器人图片
大量科技背景元素
```

更适合：

**Editorial / 编辑排版型新闻阅读页面**

特点：

- 大量留白
- 清晰排版
- 细分割线
- 少量强调色
- 信息密度适中
- 强调标题和摘要阅读体验

建议视觉参考：

```text
背景：
暖灰或浅灰白

主文字：
接近黑色

次级文字：
中性灰

分割线：
浅灰

强调色：
只使用一种
```

页面主体宽度建议控制在：

```text
800px ～ 900px
```

不要铺满整个宽屏。

---

# 七、字体层级建议

大致可以按照：

```text
页面标题
32～40px

新闻标题
20～22px
font-weight: 600

摘要
15～16px
line-height: 1.6

来源 / 类型 / 时间
12～13px
```

一条新闻尽量控制为：

```text
标题：1～2 行

摘要：2～3 行

来源信息：1 行
```

避免一条新闻占据太大高度。

---

# 八、交互保持简单

纯静态页面第一版只需要考虑：

- 按 `source_type` 过滤
- 搜索
- 日期切换（如果现有数据方便支持）
- 点击新闻打开 `canonical_url`
- 深色 / 浅色模式可以作为可选项

不需要：

- 用户系统
- 评论
- 推荐算法
- AI 分类
- 评分系统
- 收藏系统
- Dashboard
- 复杂新闻详情页

除非真实项目已经有这些能力。

---

# 九、实现原则

请不要直接照着这个方案写页面。

先检查真实 AI Radar 项目：

1. 当前前端技术栈是什么。
2. 数据文件实际放在哪里。
3. `items JSONL` 的真实字段结构。
4. `source_type` 当前有哪些实际值。
5. `published_at` / `fetched_at` 的格式。
6. `canonical_url` 是否稳定存在。
7. 当前每天大约会产生多少条新闻。
8. 是否已经有日期目录或历史数据结构。
9. 当前是否已有页面、组件或 CSS，可以复用而不是全部推倒重做。

然后基于真实项目判断：

- 页面应该如何组织。
- 哪些字段适合展示。
- 哪些分类确实可用。
- 是否需要日期切换。
- 是否需要搜索。
- 桌面端和手机端分别如何布局。

如果真实项目情况和本文方案冲突：

**优先以真实项目结构和真实数据为准。**

---

# 十、最终目标

第一版前端最重要的目标不是功能多。

而是：

> 打开页面后，可以快速、舒服地连续浏览当天抓取到的 AI 新闻。

核心信息层级只有：

```text
标题
↓
短摘要
↓
来源 · 来源类型 · 发布时间
```

其他功能都应该围绕这个核心服务，而不是让页面变复杂。
